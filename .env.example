# Deployed backend URL — required for Capacitor / native builds (no Vite proxy).
# Example: https://latvian-teacher.fly.dev
VITE_API_BASE_URL=

# Optional — must match server API_ACCESS_TOKEN when API is protected
VITE_API_ACCESS_TOKEN=
